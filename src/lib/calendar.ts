/**
 * Kalendarz wydarzeń uczelnianych (/kalendarz).
 *
 * Wklej publiczny adres osadzenia Google Calendar uczelni:
 * Google Calendar → Ustawienia kalendarza → „Zintegruj kalendarz" →
 * „Kod do umieszczenia" → skopiuj wartość atrybutu `src` z <iframe …>.
 * PUSTE = strona pokazuje stan „Wkrótce".
 */
export const universityCalendarEmbedUrl = "";
