---
phase: 03-strona-g-wna-strefa-studenta
plan: 05
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - app/(public)/infopacki/page.tsx
  - app/(public)/rzecznik-praw-studenta/page.tsx
  - app/(public)/stypendia/page.tsx
autonomous: true
requirements:
  - STU-03
  - STU-05
  - STU-06
  - STU-07

must_haves:
  truths:
    - "/infopacki renders 8 infopack links from data/infopacki.ts via DocumentList"
    - "/rzecznik-praw-studenta renders MemberCard for Jakub Buchta + description sections + UI-only form"
    - "/stypendia renders 4 tile links + table of 10 attachments with GDrive URLs"
    - "All GDrive links open in new tab with rel='noopener noreferrer'"
    - "Rzecznik form has dropdown, textarea, email input — no POST action (Phase 5 wires backend)"
  artifacts:
    - path: "app/(public)/infopacki/page.tsx"
      provides: "Infopacki page with DocumentList of 8 entries"
      exports: ["default (InfopackiPage)"]
    - path: "app/(public)/rzecznik-praw-studenta/page.tsx"
      provides: "Rzecznik page with MemberCard, responsibilities, UI form"
      exports: ["default (RzecznikPage)"]
    - path: "app/(public)/stypendia/page.tsx"
      provides: "Stypendia page with 4 navigation tiles + 10 attachment links"
      exports: ["default (StypendiaPage)"]
  key_links:
    - from: "app/(public)/infopacki/page.tsx"
      to: "data/infopacki.ts"
      via: "imports infopacki, maps to DocumentItem[] for DocumentList"
      pattern: "infopacki"
    - from: "app/(public)/rzecznik-praw-studenta/page.tsx"
      to: "components/cards/MemberCard.tsx"
      via: "renders MemberCard with Jakub Buchta data"
      pattern: "MemberCard"
    - from: "app/(public)/stypendia/page.tsx"
      to: "data/stypendia.ts"
      via: "imports stypendiaAttachments, maps to DocumentItem[] for DocumentList"
      pattern: "stypendiaAttachments"
---

<objective>
Build three content-heavy Strefa Studenta subpages: /infopacki (GDrive document links),
/rzecznik-praw-studenta (member card + UI form), and /stypendia (tiles + attachments table).

Purpose: Depends on Wave 1 data files (03-01) and components (03-02). Can execute in
parallel with Plan 03-04 in Wave 2.
Output: 3 page files composing Wave 1 components with full content from brief.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-strona-g-wna-strefa-studenta/03-CONTEXT.md
@.planning/phases/02-next-js-foundation/02-UI-SPEC.md
@tailwind.config.js
@components/ui/FadeUp.tsx
@components/ui/Button.tsx
@.planning/phases/03-strona-g-wna-strefa-studenta/03-01-SUMMARY.md
@.planning/phases/03-strona-g-wna-strefa-studenta/03-02-SUMMARY.md

<interfaces>
<!-- Components and data from Wave 1 that these pages consume -->

From data/infopacki.ts (03-01):
```ts
export interface Infopack { title: string; href: string }
export const infopacki: Infopack[]  // 8 entries
```

From data/stypendia.ts (03-01):
```ts
export interface StypendiaTile { title: string; href: string }
export interface StypendiumAttachment { number: string; description: string; href: string }
export const stypendiaTiles: StypendiaTile[]        // 4 entries
export const stypendiaAttachments: StypendiumAttachment[]  // 10 entries
```

From components/sections/DocumentList.tsx (03-02):
```tsx
export interface DocumentItem { label?: string; description: string; href: string }
export function DocumentList({ items, className }: { items: DocumentItem[]; className?: string }): JSX.Element
// Server Component — renders list of labeled external links
```

From components/cards/MemberCard.tsx (03-02):
```tsx
export function MemberCard({ initials, name, role, email, description, className }: MemberCardProps): JSX.Element
// Server Component — renders initials avatar + name + role + email
```

From components/ui/FadeUp.tsx:
```tsx
export function FadeUp({ children, delay = 0, className }: FadeUpProps): JSX.Element
```

From components/cards/TileCard.tsx (03-01):
```tsx
export function TileCard({ title, iconName, href }: TileCardProps): JSX.Element
// 'iconName' is a Lucide component name string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /infopacki and /rzecznik-praw-studenta pages</name>
  <files>app/(public)/infopacki/page.tsx, app/(public)/rzecznik-praw-studenta/page.tsx</files>
  <action>
    **`app/(public)/infopacki/page.tsx`:**
    ```tsx
    import type { Metadata } from 'next'
    import Link from 'next/link'
    import { FadeUp } from '@/components/ui/FadeUp'
    import { DocumentList, DocumentItem } from '@/components/sections/DocumentList'
    import { infopacki } from '@/data/infopacki'

    export const metadata: Metadata = {
      title: 'Infopacki — SSUEW',
      description: 'Przewodniki dla studentów UEW: USOS, regulamin studiów, dyplomowanie i więcej.',
    }

    export default function InfopackiPage() {
      const items: DocumentItem[] = infopacki.map((ip) => ({
        description: ip.title,
        href: ip.href,
      }))

      return (
        <main>
          {/* Subpage hero */}
          <section className="pt-36 pb-16 bg-gradient-to-br from-ssuew-gray-100 to-white">
            <div className="max-w-brand mx-auto px-6">
              <FadeUp>
                <h1 className="font-display text-display-xl text-ssuew-black mb-4">
                  Infopacki
                </h1>
                <p className="text-base text-ssuew-gray-600 max-w-2xl leading-relaxed">
                  W tym miejscu znajdziesz przygotowane dla studentów naszej uczelni infopacki.
                  Pozwalają one na zrozumienie działania różnych regulaminów, podań oraz na
                  swobodne poruszanie się w przestrzeni online na naszym uniwersytecie.
                </p>
              </FadeUp>
            </div>
          </section>

          {/* Document list */}
          <section className="py-20 md:py-14 bg-white">
            <div className="max-w-brand mx-auto px-6 max-w-3xl">
              <FadeUp>
                <DocumentList items={items} />
              </FadeUp>
            </div>
          </section>

          {/* CTA + back */}
          <section className="py-12 bg-ssuew-gray-100">
            <div className="max-w-brand mx-auto px-6 flex flex-col sm:flex-row items-center justify-between gap-4">
              <p className="text-[0.85rem] text-ssuew-gray-600">
                Jeżeli nie znalazłeś odpowiedzi na swoje pytanie zapraszamy do kontaktu!{' '}
                <Link href="/formularz" className="text-primary font-bold hover:text-primary-dark transition-colors">
                  Napisz do nas →
                </Link>
              </p>
              <Link
                href="/dla-studenta"
                className="inline-flex items-center gap-2 px-5 py-2 rounded-full border-2 border-ssuew-gray-200 text-ssuew-gray-600 text-[0.85rem] font-bold hover:border-primary hover:text-primary transition-all duration-200"
              >
                ← WRÓĆ
              </Link>
            </div>
          </section>
        </main>
      )
    }
    ```

    **`app/(public)/rzecznik-praw-studenta/page.tsx`:**
    ```tsx
    import type { Metadata } from 'next'
    import Link from 'next/link'
    import { FadeUp } from '@/components/ui/FadeUp'
    import { MemberCard } from '@/components/cards/MemberCard'

    export const metadata: Metadata = {
      title: 'Rzecznik Praw Studenta — SSUEW',
      description: 'Jakub Buchta, Rzecznik Praw Studenta UEW. Pomoc w interpretacji przepisów i wsparcie w trudnych sytuacjach.',
    }

    export default function RzecznikPage() {
      return (
        <main>
          {/* Subpage hero */}
          <section className="pt-36 pb-16 bg-gradient-to-br from-ssuew-gray-100 to-white">
            <div className="max-w-brand mx-auto px-6">
              <FadeUp>
                <h1 className="font-display text-display-xl text-ssuew-black mb-4">
                  Rzecznik Praw Studenta
                </h1>
                <p className="text-base text-ssuew-gray-600 max-w-2xl leading-relaxed">
                  Rzecznik Praw Studenta wspiera studentów w interpretacji przepisów i jest
                  reprezentantem studentów w kontakcie z odpowiednimi jednostkami
                  administracyjnymi uczelni.
                </p>
              </FadeUp>
            </div>
          </section>

          {/* Member card */}
          <section className="py-16 bg-white">
            <div className="max-w-brand mx-auto px-6 max-w-3xl">
              <FadeUp>
                <MemberCard
                  initials="JB"
                  name="Jakub Buchta"
                  role="Rzecznik Praw Studenta"
                  email="rps@samorzad.ue.wroc.pl"
                />
              </FadeUp>
            </div>
          </section>

          {/* Czym się zajmuję */}
          <section className="py-16 bg-ssuew-gray-100">
            <div className="max-w-brand mx-auto px-6 max-w-3xl">
              <FadeUp>
                <h2 className="font-display text-display-lg text-ssuew-black mb-6">
                  Czym się zajmuję
                </h2>
                <ul className="flex flex-col gap-3">
                  {[
                    'Pomoc w interpretacji przepisów i regulaminów uczelni.',
                    'Wsparcie w sytuacjach niesprawiedliwości, gdy kontakt z dziekanatem, prodziekanem lub innymi jednostkami nie przyniósł rozwiązania.',
                    'Konsultacje w trudnych przypadkach, dotyczących praw i obowiązków studenta.',
                    'Wskazanie odpowiednich zasobów informacyjnych dostępnych na stronie Uczelni lub Samorządu Studentów.',
                  ].map((item, i) => (
                    <li key={i} className="flex items-start gap-3 text-[0.85rem] text-ssuew-gray-600 leading-relaxed">
                      <span className="shrink-0 w-5 h-5 rounded-full bg-primary-light text-primary flex items-center justify-center text-[0.75rem] font-bold mt-0.5">
                        {i + 1}
                      </span>
                      {item}
                    </li>
                  ))}
                </ul>
              </FadeUp>
            </div>
          </section>

          {/* Jak mogę ci pomóc */}
          <section className="py-16 bg-white">
            <div className="max-w-brand mx-auto px-6 max-w-3xl">
              <FadeUp>
                <h2 className="font-display text-display-lg text-ssuew-black mb-4">
                  Jak mogę Ci pomóc
                </h2>
                <p className="text-base text-ssuew-gray-600 leading-relaxed mb-6">
                  Jeśli: nie wiesz, jak interpretować regulamin / nie udało Ci się rozwiązać
                  sprawy w dziekanacie / masz wątpliwości co do swoich praw jako studenta —{' '}
                  <strong className="text-ssuew-black">NAPISZ DO MNIE!</strong>
                </p>
                <div className="p-6 bg-ssuew-gray-100 rounded-brand border border-ssuew-gray-200 text-[0.85rem] text-ssuew-gray-600 leading-relaxed">
                  <strong className="text-ssuew-black">Ważne:</strong> Zanim się skontaktujesz,
                  zapoznaj się z{' '}
                  <Link href="/infopacki" className="text-primary font-bold hover:text-primary-dark transition-colors">
                    INFOPACKAMI
                  </Link>
                  {' '}oraz{' '}
                  <a
                    href="https://uew.pl/wp-content/uploads/2024/10/UCHWALA-SENATU-NR-R.0000.18.2024-Regulamin-Studiow.pdf"
                    target="_blank"
                    rel="noopener noreferrer"
                    className="text-primary font-bold hover:text-primary-dark transition-colors"
                  >
                    REGULAMINEM STUDIÓW
                  </a>
                  .
                </div>
              </FadeUp>
            </div>
          </section>

          {/* Contact form — UI only, Phase 5 wires POST */}
          <section className="py-16 bg-ssuew-gray-100">
            <div className="max-w-brand mx-auto px-6 max-w-2xl">
              <FadeUp>
                <h2 className="font-display text-display-lg text-ssuew-black mb-8">
                  Skontaktuj się
                </h2>
                {/* NOTE: This form has no action/method — Phase 5 will add POST to /api/formularz */}
                <form className="flex flex-col gap-6">
                  <div className="flex flex-col gap-2">
                    <label className="text-[0.85rem] font-bold text-ssuew-black">
                      Wybierz problem
                    </label>
                    <select className="w-full px-4 py-3 rounded-brand-sm border border-ssuew-gray-200 text-base text-ssuew-black bg-white focus:outline-none focus:ring-2 focus:ring-primary">
                      <option value="">-- Wybierz kategorię --</option>
                      <option value="regulamin">Regulamin studiów</option>
                      <option value="dziekanat">Dziekanat</option>
                      <option value="prawa">Prawa studenta</option>
                      <option value="inne">Inne</option>
                    </select>
                  </div>

                  <div className="flex flex-col gap-2">
                    <label className="text-[0.85rem] font-bold text-ssuew-black">
                      Opis problemu
                    </label>
                    <textarea
                      rows={5}
                      placeholder="Opisz swoją sytuację..."
                      className="w-full px-4 py-3 rounded-brand-sm border border-ssuew-gray-200 text-base text-ssuew-black bg-white resize-y focus:outline-none focus:ring-2 focus:ring-primary"
                    />
                  </div>

                  <div className="flex flex-col gap-2">
                    <label className="text-[0.85rem] font-bold text-ssuew-black">
                      Twój adres email
                    </label>
                    <input
                      type="email"
                      placeholder="student@student.ue.wroc.pl"
                      className="w-full px-4 py-3 rounded-brand-sm border border-ssuew-gray-200 text-base text-ssuew-black bg-white focus:outline-none focus:ring-2 focus:ring-primary"
                    />
                  </div>

                  <button
                    type="button"
                    disabled
                    title="Formularz będzie aktywny wkrótce"
                    className="inline-flex items-center justify-center gap-2 font-bold rounded-full px-8 py-3.5 text-base bg-ssuew-gray-200 text-ssuew-gray-600 cursor-not-allowed"
                  >
                    Prześlij (wkrótce)
                  </button>
                </form>
              </FadeUp>
            </div>
          </section>

          {/* Back link */}
          <section className="py-8 bg-white">
            <div className="max-w-brand mx-auto px-6">
              <Link
                href="/dla-studenta"
                className="inline-flex items-center gap-2 px-5 py-2 rounded-full border-2 border-ssuew-gray-200 text-ssuew-gray-600 text-[0.85rem] font-bold hover:border-primary hover:text-primary transition-all duration-200"
              >
                ← WRÓĆ
              </Link>
            </div>
          </section>
        </main>
      )
    }
    ```
  </action>
  <verify>
    <automated>ls "app/(public)/infopacki/page.tsx" "app/(public)/rzecznik-praw-studenta/page.tsx" && grep "DocumentList" "app/(public)/infopacki/page.tsx" && grep "infopacki" "app/(public)/infopacki/page.tsx" && grep "MemberCard" "app/(public)/rzecznik-praw-studenta/page.tsx" && grep "rps@samorzad" "app/(public)/rzecznik-praw-studenta/page.tsx"</automated>
  </verify>
  <done>
    - app/(public)/infopacki/page.tsx imports infopacki from data/infopacki and renders DocumentList
    - /infopacki has WRÓĆ link to /dla-studenta and CTA link to /formularz
    - app/(public)/rzecznik-praw-studenta/page.tsx imports and renders MemberCard with JB initials
    - MemberCard receives email: "rps@samorzad.ue.wroc.pl"
    - Rzecznik page has UI-only form (button type="button" disabled, no action/method on form)
    - Rzecznik form has: dropdown (4 options), textarea, email input
    - Regulamin studiów link: target="_blank" rel="noopener noreferrer"
  </done>
</task>

<task type="auto">
  <name>Task 2: /stypendia page</name>
  <files>app/(public)/stypendia/page.tsx</files>
  <action>
    **`app/(public)/stypendia/page.tsx`:**
    ```tsx
    import type { Metadata } from 'next'
    import Link from 'next/link'
    import { FadeUp } from '@/components/ui/FadeUp'
    import { DocumentList, DocumentItem } from '@/components/sections/DocumentList'
    import { stypendiaTiles, stypendiaAttachments } from '@/data/stypendia'

    export const metadata: Metadata = {
      title: 'Stypendia — SSUEW',
      description: 'Informacje o stypendiach dla studentów UEW: socjalne, rektora, dla niepełnosprawnych, zapomogi. Dokumenty do pobrania.',
    }

    export default function StypendiaPage() {
      const attachmentItems: DocumentItem[] = stypendiaAttachments.map((att) => ({
        label: att.number,
        description: att.description,
        href: att.href,
      }))

      return (
        <main>
          {/* Subpage hero */}
          <section className="pt-36 pb-16 bg-gradient-to-br from-ssuew-gray-100 to-white">
            <div className="max-w-brand mx-auto px-6">
              <FadeUp>
                <h1 className="font-display text-display-xl text-ssuew-black mb-4">
                  Stypendia
                </h1>
                <p className="text-base text-ssuew-gray-600 max-w-2xl leading-relaxed">
                  W tym miejscu znajdziesz kluczowe informacje dotyczące aktualności stypendiów
                  a także wszystkie niezbędne dokumenty.
                </p>
                <a
                  href="https://uew.pl/kandydaci/wsparcie-finansowe-dla-studentow/"
                  target="_blank"
                  rel="noopener noreferrer"
                  className="inline-block mt-4 text-[0.85rem] text-primary font-bold hover:text-primary-dark transition-colors"
                >
                  Aktualności stypendialne na uew.pl →
                </a>
              </FadeUp>
            </div>
          </section>

          {/* Description */}
          <section className="py-12 bg-white">
            <div className="max-w-brand mx-auto px-6 max-w-3xl">
              <FadeUp>
                <p className="text-base text-ssuew-gray-600 leading-relaxed">
                  Podczas studiów możesz skorzystać z różnych form wsparcia finansowego
                  oferowanych przez uczelnię. Obejmują one zarówno świadczenia dla studentów
                  znajdujących się w trudniejszej sytuacji materialnej, jak i stypendia
                  przyznawane za bardzo dobre wyniki w nauce oraz osiągnięcia naukowe,
                  sportowe lub artystyczne.
                </p>
              </FadeUp>
            </div>
          </section>

          {/* 4 type tiles */}
          <section className="py-16 bg-ssuew-gray-100">
            <div className="max-w-brand mx-auto px-6">
              <FadeUp>
                <h2 className="font-display text-display-lg text-ssuew-black mb-8 text-center">
                  Rodzaje stypendiów
                </h2>
              </FadeUp>
              <div className="grid grid-cols-2 md:grid-cols-4 gap-6">
                {stypendiaTiles.map((tile, i) => (
                  <FadeUp key={tile.href} delay={i * 0.1}>
                    <Link
                      href={tile.href}
                      className="flex flex-col items-center justify-center gap-3 p-8 bg-white rounded-brand border border-ssuew-gray-200 shadow-brand hover:border-primary hover:shadow-brand-hover hover:-translate-y-1 transition-all duration-300 text-center"
                    >
                      <span className="text-[0.85rem] font-bold text-ssuew-black tracking-wide uppercase">
                        {tile.title}
                      </span>
                    </Link>
                  </FadeUp>
                ))}
              </div>
            </div>
          </section>

          {/* Attachments */}
          <section className="py-20 md:py-14 bg-white">
            <div className="max-w-brand mx-auto px-6 max-w-3xl">
              <FadeUp>
                <h2 className="font-display text-display-lg text-ssuew-black mb-8">
                  Dokumenty do pobrania
                </h2>
                <DocumentList items={attachmentItems} />
              </FadeUp>
            </div>
          </section>

          {/* Back link */}
          <section className="py-8 bg-ssuew-gray-100">
            <div className="max-w-brand mx-auto px-6">
              <Link
                href="/dla-studenta"
                className="inline-flex items-center gap-2 px-5 py-2 rounded-full border-2 border-ssuew-gray-200 text-ssuew-gray-600 text-[0.85rem] font-bold hover:border-primary hover:text-primary transition-all duration-200"
              >
                ← WRÓĆ
              </Link>
            </div>
          </section>
        </main>
      )
    }
    ```
  </action>
  <verify>
    <automated>ls "app/(public)/stypendia/page.tsx" && grep "stypendiaAttachments" "app/(public)/stypendia/page.tsx" && grep "DocumentList" "app/(public)/stypendia/page.tsx" && grep "stypendiaTiles" "app/(public)/stypendia/page.tsx" && grep "wsparcie-finansowe" "app/(public)/stypendia/page.tsx"</automated>
  </verify>
  <done>
    - app/(public)/stypendia/page.tsx imports stypendiaTiles (4) and stypendiaAttachments (10) from data/stypendia
    - Page renders 4 navigation tiles (Socjalne, Rektora, Dla Niepełnosprawnych, Zapomogi)
    - Page renders DocumentList with 10 attachments (label prefix "Zał. N")
    - Link to uew.pl/kandydaci/wsparcie-finansowe... with target="_blank"
    - WRÓĆ link to /dla-studenta
    - No 'use client' directive
    - npm run build exits 0 after all 3 pages created
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. npm run build → exits 0
2. grep "drive.google.com" "app/(public)/infopacki/page.tsx" → no match (data comes from data/infopacki.ts)
3. grep "infopacki" "app/(public)/infopacki/page.tsx" → match (imports from data)
4. grep "rps@samorzad.ue.wroc.pl" "app/(public)/rzecznik-praw-studenta/page.tsx" → match
5. grep "type=\"button\"" "app/(public)/rzecznik-praw-studenta/page.tsx" → match (submit is disabled button)
6. grep "stypendiaAttachments" "app/(public)/stypendia/page.tsx" → match
7. grep "stypendiaTiles" "app/(public)/stypendia/page.tsx" → match
8. grep "wsparcie-finansowe" "app/(public)/stypendia/page.tsx" → match
9. grep "target.*_blank" "app/(public)/rzecznik-praw-studenta/page.tsx" → match
10. grep "use client" "app/(public)/stypendia/page.tsx" → no match
</verification>

<success_criteria>
- /infopacki renders 8 DocumentList items from data/infopacki.ts — all with GDrive URLs
- /rzecznik-praw-studenta has MemberCard (JB, rps@...) + 4 responsibility bullets + UI-only form
- Rzecznik form submit button is disabled (type="button", no form action) — Phase 5 wires POST
- /stypendia has 4 tile links + DocumentList of 10 attachments with "Zał. N" label prefixes
- All 3 pages are Server Components with no 'use client'
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-strona-g-wna-strefa-studenta/03-05-SUMMARY.md`
</output>
