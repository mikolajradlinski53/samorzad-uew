---
phase: 03-strona-g-wna-strefa-studenta
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-03
files_modified:
  - app/(public)/page.tsx
  - app/(public)/dla-studenta/page.tsx
  - app/(public)/prawa-studenta/page.tsx
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - STU-01
  - STU-02
  - STU-04

must_haves:
  truths:
    - "/ (homepage) renders HeroSection, StatsSection, AboutSection in sequence"
    - "/dla-studenta renders intro text, TileGrid with 10 tiles, and QuickLinks row"
    - "/prawa-studenta renders intro paragraph, Accordion with 7 items, link to Ustawa PSW, and 'WRÓĆ' button"
    - "All three pages are Server Components — no 'use client' on the page files"
  artifacts:
    - path: "app/(public)/page.tsx"
      provides: "Homepage composing HeroSection + StatsSection + AboutSection"
      exports: ["default (HomePage)"]
    - path: "app/(public)/dla-studenta/page.tsx"
      provides: "Student zone hub page with TileGrid + QuickLinks"
      exports: ["default (DlaStudentaPage)"]
    - path: "app/(public)/prawa-studenta/page.tsx"
      provides: "Student rights page with Accordion of 7 rights"
      exports: ["default (PrawaStudentaPage)"]
  key_links:
    - from: "app/(public)/page.tsx"
      to: "components/sections/HeroSection.tsx"
      via: "direct import and render"
      pattern: "HeroSection"
    - from: "app/(public)/dla-studenta/page.tsx"
      to: "components/sections/TileGrid.tsx"
      via: "direct import and render"
      pattern: "TileGrid"
    - from: "app/(public)/prawa-studenta/page.tsx"
      to: "data/prawa-studenta.ts"
      via: "imports prawaNr, passes to Accordion items prop"
      pattern: "prawaNr"
---

<objective>
Build the homepage and two fully-contentful Strefa Studenta subpages: /dla-studenta (tile hub)
and /prawa-studenta (accordion of rights).

Purpose: Depends on Wave 1 components from plans 03-01 and 03-03. These are the first
publicly-facing pages that use all the Wave 1 building blocks.
Output: 3 page files that compose Wave 1 components with real content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-strona-g-wna-strefa-studenta/03-CONTEXT.md
@.planning/phases/02-next-js-foundation/02-UI-SPEC.md
@tailwind.config.js
@components/ui/FadeUp.tsx
@components/ui/Button.tsx
@.planning/phases/03-strona-g-wna-strefa-studenta/03-01-SUMMARY.md
@.planning/phases/03-strona-g-wna-strefa-studenta/03-03-SUMMARY.md

<interfaces>
<!-- Components created in Wave 1 that these pages consume -->

From components/sections/HeroSection.tsx (03-03):
```tsx
export function HeroSection(): JSX.Element
// Renders hero with H1, rotating subtitles (AnimatePresence), CTA buttons, social icons
// Self-contained — no props needed
```

From components/sections/StatsSection.tsx (03-03):
```tsx
export function StatsSection(): JSX.Element
// Renders 4 stat cards with animated Counter components
// Self-contained — no props needed
```

From components/sections/AboutSection.tsx (03-03):
```tsx
export function AboutSection(): JSX.Element
// Renders "O nas" section with exact paragraph text
// Self-contained — no props needed
```

From components/sections/TileGrid.tsx (03-01):
```tsx
export function TileGrid(): JSX.Element
// Renders responsive grid of 10 TileCards — reads tiles from data/student-zone.ts directly
// Self-contained — no props needed
```

From components/sections/QuickLinks.tsx (03-01):
```tsx
export function QuickLinks(): JSX.Element
// Renders row of 4 external quick-link buttons — reads quickLinks from data/student-zone.ts
// Self-contained — no props needed
```

From components/sections/Accordion.tsx (03-02):
```tsx
export interface AccordionItem { title: string; content: string }
export function Accordion({ items }: { items: AccordionItem[] }): JSX.Element
// 'use client' — handles expand/collapse state internally
```

From data/prawa-studenta.ts (03-01):
```ts
export interface PrawoStudenta { title: string; content: string }
export const prawaNr: PrawoStudenta[]  // 7 items
```

From components/ui/FadeUp.tsx:
```tsx
export function FadeUp({ children, delay = 0, className }: FadeUpProps): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Homepage — app/(public)/page.tsx</name>
  <files>app/(public)/page.tsx</files>
  <action>
    Create the (public) route group directory structure if it does not exist.
    The path `app/(public)/` creates a route group — pages inside have URLs without the
    "(public)" prefix (e.g., `app/(public)/page.tsx` serves `/`).

    IMPORTANT: The existing `app/page.tsx` at the root must be REPLACED by
    `app/(public)/page.tsx`. Delete or overwrite `app/page.tsx` with a redirect
    or move it. The cleanest approach: create `app/(public)/page.tsx` as the homepage
    and remove `app/page.tsx` (or make it re-export), ensuring Next.js serves
    the (public) group's page.tsx as `/`.

    Actually: in Next.js App Router, `app/(public)/page.tsx` and `app/page.tsx` BOTH
    map to `/`. Having both causes a conflict. So:
    1. Create the file `app/(public)/page.tsx` with homepage content
    2. Delete `app/page.tsx` (the current placeholder)

    **`app/(public)/page.tsx`:**
    ```tsx
    import type { Metadata } from 'next'
    import { HeroSection } from '@/components/sections/HeroSection'
    import { StatsSection } from '@/components/sections/StatsSection'
    import { AboutSection } from '@/components/sections/AboutSection'

    export const metadata: Metadata = {
      title: 'Samorząd Studentów UEW — Strona Główna',
      description: 'Samorząd Studentów Uniwersytetu Ekonomicznego we Wrocławiu. Działamy na rzecz studentów, wspieramy prawa studenckie i inspirujemy do nowych inicjatyw.',
    }

    export default function HomePage() {
      return (
        <>
          <HeroSection />
          <StatsSection />
          <AboutSection />
        </>
      )
    }
    ```

    After creating, verify build still passes:
    ```bash
    npm run build
    ```
    If conflict error about duplicate `/` route: ensure app/page.tsx is deleted.
  </action>
  <verify>
    <automated>ls "app/(public)/page.tsx" && grep "HeroSection" "app/(public)/page.tsx" && grep "StatsSection" "app/(public)/page.tsx" && grep "AboutSection" "app/(public)/page.tsx"</automated>
  </verify>
  <done>
    - app/(public)/page.tsx exists
    - Imports HeroSection, StatsSection, AboutSection
    - No 'use client' directive on page file
    - app/page.tsx (old placeholder) removed or emptied to avoid route conflict
    - npm run build exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: /dla-studenta and /prawa-studenta pages</name>
  <files>app/(public)/dla-studenta/page.tsx, app/(public)/prawa-studenta/page.tsx</files>
  <action>
    **`app/(public)/dla-studenta/page.tsx`:**
    ```tsx
    import type { Metadata } from 'next'
    import { FadeUp } from '@/components/ui/FadeUp'
    import { TileGrid } from '@/components/sections/TileGrid'
    import { QuickLinks } from '@/components/sections/QuickLinks'

    export const metadata: Metadata = {
      title: 'Strefa Studenta — SSUEW',
      description: 'Wszystkie ważne informacje dla studentów UEW: prawa, stypendia, mapa kampusu, infopacki i więcej.',
    }

    export default function DlaStudentaPage() {
      return (
        <main>
          {/* Subpage hero */}
          <section className="pt-36 pb-16 bg-gradient-to-br from-ssuew-gray-100 to-white">
            <div className="max-w-brand mx-auto px-6">
              <FadeUp>
                <h1 className="font-display text-display-xl text-ssuew-black mb-4">
                  Strefa Studenta
                </h1>
                <p className="text-base text-ssuew-gray-600 max-w-2xl leading-relaxed">
                  W tym miejscu znajdziesz wszystkie najważniejsze informacje, jakie mogą Ci się
                  przydać w codziennym życiu na UEW. Kliknij w poniższe kafelki, żeby przenieść
                  się do właściwej strony.
                </p>
              </FadeUp>
            </div>
          </section>

          {/* Tile grid */}
          <section className="py-20 md:py-14 bg-white">
            <div className="max-w-brand mx-auto px-6">
              <FadeUp delay={0.1}>
                <TileGrid />
              </FadeUp>
            </div>
          </section>

          {/* Quick links */}
          <section className="py-16 bg-ssuew-gray-100">
            <div className="max-w-brand mx-auto px-6">
              <FadeUp>
                <h2 className="font-display text-display-lg text-ssuew-black text-center mb-10">
                  Szybkie linki
                </h2>
              </FadeUp>
              <FadeUp delay={0.1}>
                <QuickLinks />
              </FadeUp>
            </div>
          </section>
        </main>
      )
    }
    ```

    **`app/(public)/prawa-studenta/page.tsx`:**
    ```tsx
    import type { Metadata } from 'next'
    import Link from 'next/link'
    import { FadeUp } from '@/components/ui/FadeUp'
    import { Accordion } from '@/components/sections/Accordion'
    import { prawaNr } from '@/data/prawa-studenta'

    export const metadata: Metadata = {
      title: 'Prawa Studenta — SSUEW',
      description: 'Poznaj swoje prawa jako student UEW. Regulacje wynikające z Ustawy 2.0.',
    }

    export default function PrawaStudentaPage() {
      return (
        <main>
          {/* Subpage hero */}
          <section className="pt-36 pb-16 bg-gradient-to-br from-ssuew-gray-100 to-white">
            <div className="max-w-brand mx-auto px-6">
              <FadeUp>
                <h1 className="font-display text-display-xl text-ssuew-black mb-4">
                  Prawa Studenta
                </h1>
                <p className="text-base text-ssuew-gray-600 max-w-2xl leading-relaxed">
                  Dzięki Ustawie 2.0 (Konstytucja dla Nauki) każdy student ma prawo do
                  przeszkolenia w zakresie swoich praw i obowiązków. Za organizację i prowadzenie
                  tego szkolenia odpowiedzialny jest samorząd studencki, we współpracy z
                  Parlamentem Studentów Rzeczypospolitej Polskiej.
                </p>
              </FadeUp>
            </div>
          </section>

          {/* Accordion */}
          <section className="py-20 md:py-14 bg-white">
            <div className="max-w-brand mx-auto px-6 max-w-3xl">
              <FadeUp>
                <Accordion items={prawaNr} />
              </FadeUp>
            </div>
          </section>

          {/* Footer links */}
          <section className="py-12 bg-ssuew-gray-100">
            <div className="max-w-brand mx-auto px-6 flex flex-col sm:flex-row items-center justify-between gap-4">
              <a
                href="https://isap.sejm.gov.pl/isap.nsf/download.xsp/WDU20180001668/U/D20181668Lj.pdf"
                target="_blank"
                rel="noopener noreferrer"
                className="text-[0.85rem] text-primary hover:text-primary-dark font-bold transition-colors duration-200"
              >
                Ustawa PSW — Prawo o szkolnictwie wyższym i nauce →
              </a>
              <Link
                href="/dla-studenta"
                className="inline-flex items-center gap-2 px-5 py-2 rounded-full border-2 border-ssuew-gray-200 text-ssuew-gray-600 text-[0.85rem] font-bold hover:border-primary hover:text-primary transition-all duration-200"
              >
                ← WRÓĆ
              </Link>
            </div>
          </section>
        </main>
      )
    }
    ```
  </action>
  <verify>
    <automated>ls "app/(public)/dla-studenta/page.tsx" "app/(public)/prawa-studenta/page.tsx" && grep "TileGrid" "app/(public)/dla-studenta/page.tsx" && grep "QuickLinks" "app/(public)/dla-studenta/page.tsx" && grep "prawaNr" "app/(public)/prawa-studenta/page.tsx" && grep "Accordion" "app/(public)/prawa-studenta/page.tsx"</automated>
  </verify>
  <done>
    - app/(public)/dla-studenta/page.tsx imports and renders TileGrid and QuickLinks
    - app/(public)/prawa-studenta/page.tsx imports prawaNr from data/prawa-studenta and passes to Accordion
    - Both pages have no 'use client' directive (Server Components)
    - /prawa-studenta has external link to Ustawa PSW with target="_blank"
    - /prawa-studenta has "WRÓĆ" link back to /dla-studenta
    - Intro text matches 03-CONTEXT.md verbatim
    - npm run build exits 0 after creating both pages
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. npm run build → exits 0 (no route conflicts, all imports resolve)
2. ls "app/(public)/page.tsx" → exists
3. ls "app/(public)/dla-studenta/page.tsx" → exists
4. ls "app/(public)/prawa-studenta/page.tsx" → exists
5. grep "HeroSection" "app/(public)/page.tsx" → match
6. grep "TileGrid" "app/(public)/dla-studenta/page.tsx" → match
7. grep "prawaNr" "app/(public)/prawa-studenta/page.tsx" → match
8. grep "isap.sejm.gov.pl" "app/(public)/prawa-studenta/page.tsx" → match
9. grep "use client" "app/(public)/page.tsx" → no match
10. grep "dla-studenta" "app/(public)/prawa-studenta/page.tsx" → match (WRÓĆ link)
</verification>

<success_criteria>
- Homepage renders HeroSection + StatsSection + AboutSection (no placeholder text)
- /dla-studenta renders intro + TileGrid (10 tiles) + QuickLinks (4 external links)
- /prawa-studenta renders intro + Accordion (prawaNr 7 items) + Ustawa PSW link + WRÓĆ link
- All 3 pages are Server Components
- npm run build passes after this plan
</success_criteria>

<output>
After completion, create `.planning/phases/03-strona-g-wna-strefa-studenta/03-04-SUMMARY.md`
</output>
