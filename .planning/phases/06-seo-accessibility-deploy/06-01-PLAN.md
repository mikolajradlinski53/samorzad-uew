---
phase: 06-seo-accessibility-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/sitemap.ts
  - app/robots.ts
  - app/opengraph-image.tsx
  - app/(public)/page.tsx
  - app/(public)/dla-studenta/page.tsx
  - app/(public)/prawa-studenta/page.tsx
  - app/(public)/infopacki/page.tsx
  - app/(public)/rzecznik-praw-studenta/page.tsx
  - app/(public)/stypendia/page.tsx
  - app/(public)/zarzad/page.tsx
  - app/(public)/rada-uczelniana/page.tsx
  - app/(public)/przewodniczacy-i-wiceprzewodniczacy/page.tsx
  - app/(public)/nasze-projekty/page.tsx
  - app/(public)/wspolprace/page.tsx
  - app/(public)/regulacje-wewnetrzne/page.tsx
  - app/(public)/formularz/page.tsx
  - app/(public)/strefa-dzialacza/page.tsx
  - app/(public)/nasza-dzialalnosc/page.tsx
  - app/(public)/struktura-samorzadu/page.tsx
  - app/(public)/komisja-rewizyjna/page.tsx
  - app/(public)/studencka-komisja-wyborcza/page.tsx
  - app/(public)/nasi-partnerzy/page.tsx
  - app/(public)/filia-jelenia-gora/page.tsx
  - app/(public)/mapa-kampusu/page.tsx
  - app/(public)/wladze-rektorskie/page.tsx
  - app/(public)/dziekan-i-prodziekani/page.tsx
  - app/(public)/pomoc-psychologiczna/page.tsx
  - app/(public)/prawo-dla-studenta/page.tsx
  - app/(public)/ankiety-dydaktyczne/page.tsx
autonomous: true
requirements:
  - SEO-01

must_haves:
  truths:
    - "Sharing any SSUEW page on social media shows a rich OG preview with title, description, and branded image"
    - "Google can discover all public pages via /sitemap.xml"
    - "Bots receive crawl instructions via /robots.txt"
    - "Every page title renders as '{Page Name} | SSUEW' in browser tab"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Global metadata with OG defaults and title template"
      contains: "template: '%s | SSUEW'"
    - path: "app/sitemap.ts"
      provides: "XML sitemap for all public routes"
      exports: ["default function sitemap"]
    - path: "app/robots.ts"
      provides: "robots.txt with sitemap reference"
      exports: ["default function robots"]
    - path: "app/opengraph-image.tsx"
      provides: "Dynamic OG image (1200x630) with brand identity"
      contains: "ImageResponse"
  key_links:
    - from: "app/layout.tsx"
      to: "all page.tsx files"
      via: "Metadata template inheritance — page title overrides default"
      pattern: "template.*SSUEW"
    - from: "app/sitemap.ts"
      to: "baseUrl"
      via: "routes array maps to full URLs"
      pattern: "samorzad.ue.wroc.pl"
---

<objective>
Add complete SEO infrastructure: global OG metadata defaults in layout, dynamic OG image via Next.js ImageResponse, sitemap.xml, robots.txt, and per-page openGraph fields on all 20+ public pages.

Purpose: Every page generates a rich social preview and is discoverable by search engines.
Output: app/layout.tsx (updated), app/sitemap.ts, app/robots.ts, app/opengraph-image.tsx, all page.tsx files with openGraph fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-seo-accessibility-deploy/06-CONTEXT.md

<interfaces>
<!-- Current app/layout.tsx metadata (line 22-25): -->
```typescript
export const metadata: Metadata = {
  title: 'Samorząd Studentów UEW',
  description: 'Samorząd Studentów Uniwersytetu Ekonomicznego we Wrocławiu',
}
```

<!-- Pattern on every page.tsx (example from /dla-studenta): -->
```typescript
export const metadata: Metadata = {
  title: 'Strefa Studenta — SSUEW',
  description: 'Wszystkie ważne informacje dla studentów UEW: ...',
}
```
Note: ALL pages use this flat title format. After updating layout.tsx with a template,
page titles MUST be shortened to just the page name (e.g., 'Strefa Studenta') so the
template produces 'Strefa Studenta | SSUEW'. Do NOT keep ' — SSUEW' suffix in page titles.

<!-- next.config.js — already has images.unoptimized: true, conditional static export -->
<!-- tailwind colors: primary: '#3BAEFF', ssuew-black: '#1A2340' -->
<!-- Logo at: /assets/logos/logo.png (public/assets/logos/logo.png) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Global metadata + OG image + sitemap + robots</name>
  <files>app/layout.tsx, app/opengraph-image.tsx, app/sitemap.ts, app/robots.ts</files>
  <action>
Read app/layout.tsx first. Then make these changes:

**app/layout.tsx** — replace the metadata export (lines 22-25) with:
```typescript
export const metadata: Metadata = {
  title: {
    default: 'Samorząd Studentów UEW',
    template: '%s | SSUEW',
  },
  description: 'Samorząd Studentów Uniwersytetu Ekonomicznego we Wrocławiu. Działamy na rzecz studentów, wspieramy prawa studenckie i inspirujemy do nowych inicjatyw.',
  metadataBase: new URL('https://samorzad.ue.wroc.pl'),
  openGraph: {
    siteName: 'Samorząd Studentów UEW',
    locale: 'pl_PL',
    type: 'website',
    images: [{ url: '/opengraph-image', width: 1200, height: 630, alt: 'Samorząd Studentów UEW' }],
  },
  twitter: { card: 'summary_large_image' },
}
```

**app/opengraph-image.tsx** — create new file using Next.js ImageResponse:
```typescript
import { ImageResponse } from 'next/og'

export const runtime = 'edge'
export const alt = 'Samorząd Studentów UEW'
export const size = { width: 1200, height: 630 }
export const contentType = 'image/png'

export default function Image() {
  return new ImageResponse(
    (
      <div
        style={{
          background: 'linear-gradient(135deg, #EAF5FF 0%, #ffffff 50%, #EAF5FF 100%)',
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 24,
          fontFamily: 'sans-serif',
        }}
      >
        <div
          style={{
            width: 80,
            height: 80,
            borderRadius: '50%',
            background: '#3BAEFF',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            color: 'white',
            fontSize: 40,
            fontWeight: 'bold',
          }}
        >
          S
        </div>
        <div style={{ fontSize: 52, fontWeight: 'bold', color: '#1A2340', textAlign: 'center', maxWidth: 900 }}>
          Samorząd Studentów UEW
        </div>
        <div style={{ fontSize: 26, color: '#6B7A99', textAlign: 'center', maxWidth: 700 }}>
          Uniwersytet Ekonomiczny we Wrocławiu
        </div>
        <div
          style={{
            marginTop: 16,
            paddingLeft: 24,
            paddingRight: 24,
            paddingTop: 12,
            paddingBottom: 12,
            borderRadius: 40,
            background: '#3BAEFF',
            color: 'white',
            fontSize: 20,
            fontWeight: 'bold',
          }}
        >
          samorzad.ue.wroc.pl
        </div>
      </div>
    ),
    { ...size }
  )
}
```

**app/sitemap.ts** — create new file:
```typescript
import { MetadataRoute } from 'next'

export default function sitemap(): MetadataRoute.Sitemap {
  const baseUrl = 'https://samorzad.ue.wroc.pl'
  const routes = [
    '/',
    '/dla-studenta',
    '/prawa-studenta',
    '/infopacki',
    '/rzecznik-praw-studenta',
    '/stypendia',
    '/mapa-kampusu',
    '/wladze-rektorskie',
    '/dziekan-i-prodziekani',
    '/prawo-dla-studenta',
    '/pomoc-psychologiczna',
    '/ankiety-dydaktyczne',
    '/nasza-dzialalnosc',
    '/struktura-samorzadu',
    '/zarzad',
    '/przewodniczacy-i-wiceprzewodniczacy',
    '/rada-uczelniana',
    '/komisja-rewizyjna',
    '/studencka-komisja-wyborcza',
    '/filia-jelenia-gora',
    '/nasze-projekty',
    '/regulacje-wewnetrzne',
    '/wspolprace',
    '/nasi-partnerzy',
    '/formularz',
  ]
  return routes.map((route) => ({
    url: `${baseUrl}${route}`,
    lastModified: new Date(),
    changeFrequency: 'monthly' as const,
    priority: route === '/' ? 1 : 0.8,
  }))
}
```

**app/robots.ts** — create new file:
```typescript
import { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: { userAgent: '*', allow: '/' },
    sitemap: 'https://samorzad.ue.wroc.pl/sitemap.xml',
  }
}
```
  </action>
  <verify>
    <automated>cd C:/Users/Mikołaj/samorzad-uew && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>app/layout.tsx has title template and openGraph defaults. app/opengraph-image.tsx, app/sitemap.ts, app/robots.ts all exist and TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add openGraph field to all page.tsx files + fix title format</name>
  <files>
    app/(public)/page.tsx,
    app/(public)/dla-studenta/page.tsx,
    app/(public)/prawa-studenta/page.tsx,
    app/(public)/infopacki/page.tsx,
    app/(public)/rzecznik-praw-studenta/page.tsx,
    app/(public)/stypendia/page.tsx,
    app/(public)/zarzad/page.tsx,
    app/(public)/rada-uczelniana/page.tsx,
    app/(public)/przewodniczacy-i-wiceprzewodniczacy/page.tsx,
    app/(public)/nasze-projekty/page.tsx,
    app/(public)/wspolprace/page.tsx,
    app/(public)/regulacje-wewnetrzne/page.tsx,
    app/(public)/formularz/page.tsx,
    app/(public)/strefa-dzialacza/page.tsx,
    app/(public)/nasza-dzialalnosc/page.tsx,
    app/(public)/struktura-samorzadu/page.tsx,
    app/(public)/komisja-rewizyjna/page.tsx,
    app/(public)/studencka-komisja-wyborcza/page.tsx,
    app/(public)/nasi-partnerzy/page.tsx,
    app/(public)/filia-jelenia-gora/page.tsx,
    app/(public)/mapa-kampusu/page.tsx,
    app/(public)/wladze-rektorskie/page.tsx,
    app/(public)/dziekan-i-prodziekani/page.tsx,
    app/(public)/pomoc-psychologiczna/page.tsx,
    app/(public)/prawo-dla-studenta/page.tsx,
    app/(public)/ankiety-dydaktyczne/page.tsx
  </files>
  <action>
Read each file before editing. For every page.tsx, make TWO changes:

1. **Shorten the title** — remove ' — SSUEW' suffix so the layout template handles it.
   Before: `title: 'Strefa Studenta — SSUEW'`
   After:  `title: 'Strefa Studenta'`

2. **Add openGraph field** with title and description matching the page's existing values.

Pattern to apply to each file:
```typescript
export const metadata: Metadata = {
  title: '{Page Name}',           // shortened — no ' — SSUEW'
  description: '{existing description}',
  openGraph: {
    title: '{Page Name} | SSUEW',
    description: '{existing description}',
    url: 'https://samorzad.ue.wroc.pl{/route}',
    type: 'website',
  },
}
```

Exact per-page values:

- `app/(public)/page.tsx`: title `'Strona Główna'`, url `/`, og.title `'Strona Główna | SSUEW'`, keep existing description
- `app/(public)/dla-studenta/page.tsx`: title `'Strefa Studenta'`, url `/dla-studenta`
- `app/(public)/prawa-studenta/page.tsx`: title `'Prawa Studenta'`, url `/prawa-studenta`
- `app/(public)/infopacki/page.tsx`: title `'Infopacki'`, url `/infopacki`
- `app/(public)/rzecznik-praw-studenta/page.tsx`: title `'Rzecznik Praw Studenta'`, url `/rzecznik-praw-studenta`
- `app/(public)/stypendia/page.tsx`: title `'Stypendia'`, url `/stypendia`
- `app/(public)/zarzad/page.tsx`: title `'Zarząd'`, url `/zarzad`
- `app/(public)/rada-uczelniana/page.tsx`: title `'Rada Uczelniana'`, url `/rada-uczelniana`
- `app/(public)/przewodniczacy-i-wiceprzewodniczacy/page.tsx`: title `'Przewodniczący i Wiceprzewodniczący'`, url `/przewodniczacy-i-wiceprzewodniczacy`
- `app/(public)/nasze-projekty/page.tsx`: title `'Nasze Projekty'`, url `/nasze-projekty`
- `app/(public)/wspolprace/page.tsx`: title `'Współprace'`, url `/wspolprace`
- `app/(public)/regulacje-wewnetrzne/page.tsx`: title `'Regulacje Wewnętrzne'`, url `/regulacje-wewnetrzne`
- `app/(public)/formularz/page.tsx`: title `'Formularz Kontaktowy'`, url `/formularz`
- `app/(public)/strefa-dzialacza/page.tsx`: title `'Strefa Działacza'`, url `/strefa-dzialacza`
- `app/(public)/nasza-dzialalnosc/page.tsx`: title `'Nasza Działalność'`, url `/nasza-dzialalnosc`
- `app/(public)/struktura-samorzadu/page.tsx`: title `'Struktura Samorządu'`, url `/struktura-samorzadu`
- `app/(public)/komisja-rewizyjna/page.tsx`: title `'Komisja Rewizyjna'`, url `/komisja-rewizyjna`
- `app/(public)/studencka-komisja-wyborcza/page.tsx`: title `'Studencka Komisja Wyborcza'`, url `/studencka-komisja-wyborcza`
- `app/(public)/nasi-partnerzy/page.tsx`: title `'Nasi Partnerzy'`, url `/nasi-partnerzy`
- `app/(public)/filia-jelenia-gora/page.tsx`: title `'Filia Jelenia Góra'`, url `/filia-jelenia-gora`
- `app/(public)/mapa-kampusu/page.tsx`: title `'Mapa Kampusu'`, url `/mapa-kampusu`
- `app/(public)/wladze-rektorskie/page.tsx`: title `'Władze Rektorskie'`, url `/wladze-rektorskie`
- `app/(public)/dziekan-i-prodziekani/page.tsx`: title `'Dziekan i Prodziekani'`, url `/dziekan-i-prodziekani`
- `app/(public)/pomoc-psychologiczna/page.tsx`: title `'Pomoc Psychologiczna'`, url `/pomoc-psychologiczna`
- `app/(public)/prawo-dla-studenta/page.tsx`: title `'Prawo dla Studenta'`, url `/prawo-dla-studenta`
- `app/(public)/ankiety-dydaktyczne/page.tsx`: title `'Ankiety Dydaktyczne'`, url `/ankiety-dydaktyczne`

Do NOT add strefa-dzialacza/dashboard or strefa-dzialacza/lista-obecnosci to the sitemap (authenticated pages).
  </action>
  <verify>
    <automated>cd C:/Users/Mikołaj/samorzad-uew && grep -r "openGraph" app/\(public\) --include="*.tsx" | wc -l</automated>
  </verify>
  <done>All 26 public page.tsx files have an openGraph field. grep returns 26 matches. Title values no longer contain ' — SSUEW'. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
```bash
# 1. TypeScript compiles
cd C:/Users/Mikołaj/samorzad-uew && npx tsc --noEmit

# 2. OG image route exists
ls app/opengraph-image.tsx

# 3. Sitemap + robots exist
ls app/sitemap.ts app/robots.ts

# 4. All pages have openGraph
grep -r "openGraph" app/(public) --include="*.tsx" | wc -l
# expect: 26

# 5. No page title still has ' — SSUEW'
grep -r "title:.*— SSUEW" app/(public) --include="*.tsx"
# expect: no output

# 6. Layout has title template
grep "template.*SSUEW" app/layout.tsx
# expect: match found
```
</verification>

<success_criteria>
- app/layout.tsx metadataBase, title template `'%s | SSUEW'`, openGraph defaults with `/opengraph-image` image URL
- app/opengraph-image.tsx renders 1200x630 branded image via ImageResponse (edge runtime)
- app/sitemap.ts exports 25 routes, all pointing to samorzad.ue.wroc.pl
- app/robots.ts points to sitemap URL
- All 26 public pages have `openGraph: { title, description, url, type }` field
- `npx tsc --noEmit` passes with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-seo-accessibility-deploy/06-01-SUMMARY.md`
</output>
