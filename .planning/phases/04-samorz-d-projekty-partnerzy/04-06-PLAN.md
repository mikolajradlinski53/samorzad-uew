---
phase: 04-samorz-d-projekty-partnerzy
plan: "06"
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - app/(public)/wspolprace/page.tsx
autonomous: true
requirements: [PART-01, ORG-04]

must_haves:
  truths:
    - "/wspolprace renders Karol Vogel's MemberCard with correct email and phone"
    - "A 'Dlaczego warto' section lists 5 bullet points from CONTEXT.md"
    - "A cooperation form UI is rendered with 6 fields: Nazwa firmy, Imię i nazwisko, Email, Telefon, Rodzaj współpracy (select), Dodatkowe informacje"
    - "Form submit button is disabled with aria-disabled=true (Phase 5 wires POST)"
    - "Partner logos section renders BNY as strategic + 11 regular partners via PartnerGrid"
  artifacts:
    - path: "app/(public)/wspolprace/page.tsx"
      provides: "Współprace page"
      exports: ["default"]
  key_links:
    - from: "data/partnerzy.ts"
      to: "app/(public)/wspolprace/page.tsx"
      via: "import partnerStrategiczny, partnerzy"
      pattern: "import.*partnerzy.*from.*data/partnerzy"
    - from: "data/zarzad.ts"
      to: "app/(public)/wspolprace/page.tsx"
      via: "Karol Vogel inline member card"
      pattern: "karol.vogel@samorzad"
---

<objective>
Build the Współprace page: contact card for Karol Vogel, "Dlaczego warto" benefits, cooperation form (UI-only), and partner logos.

Purpose: Delivers PART-01 and ORG-04 — the partnership page is the main commercial-facing page; form submit is intentionally disabled until Phase 5 API routes.
Output: app/(public)/wspolprace/page.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-samorz-d-projekty-partnerzy/04-CONTEXT.md
@.planning/phases/02-next-js-foundation/02-UI-SPEC.md
@components/cards/MemberCard.tsx
@components/sections/PartnerGrid.tsx
@components/ui/Button.tsx
@components/ui/FadeUp.tsx
</context>

<interfaces>
<!-- MemberCard (Phase 3, unchanged) -->
```typescript
interface MemberCardProps {
  initials: string; name: string; role: string; email: string; description?: string; className?: string
}
export function MemberCard(props: MemberCardProps): JSX.Element
```

<!-- PartnerGrid (from 04-02) -->
```typescript
interface PartnerGridProps { partners: Partner[]; className?: string }
export function PartnerGrid(props: PartnerGridProps): JSX.Element
```

<!-- partnerStrategiczny, partnerzy from data/partnerzy.ts (04-01) -->
```typescript
export const partnerStrategiczny: Partner  // { name: 'BNY', tier: 'strategic' }
export const partnerzy: Partner[]           // 11 regular partners
```

<!-- Button (Phase 2) -->
```typescript
interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: 'primary' | 'outline'; size?: 'default' | 'sm'
}
export function Button(props: ButtonProps): JSX.Element
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create /wspolprace page — contact, benefits, form</name>
  <files>app/(public)/wspolprace/page.tsx</files>
  <action>
Server component (no 'use client'). Create directory `app/(public)/wspolprace/` and page.tsx.

**Section 1: Subpage hero** — `<section className="pt-36 pb-16 bg-ssuew-gray-100">`
- H1: "Współprace" — `font-display text-display-xl text-ssuew-black`
- Subtitle: "Nawiąż współpracę z Samorządem Studentów UEW i dotrzyj do ponad 6000 studentów Uniwersytetu Ekonomicznego we Wrocławiu."

**Section 2: Contact + Dlaczego warto** — `<section className="py-20">`
- 2-column grid: `<div className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-start">`

Left column — Karol Vogel card:
```tsx
<div>
  <h2 className="font-display text-display-lg text-ssuew-black mb-8">Osoba kontaktowa</h2>
  <MemberCard
    initials="KV"
    name="Karol Vogel"
    role="Członek Zarządu ds. Kontaktów Zewnętrznych"
    email="karol.vogel@samorzad.ue.wroc.pl"
    description="Tel: 511 599 376 · LinkedIn: linkedin.com/in/karol-vogel-ba4896270"
  />
</div>
```

Right column — "Dlaczego warto" benefits list:
```tsx
<div>
  <h2 className="font-display text-display-lg text-ssuew-black mb-8">Dlaczego warto z nami współpracować?</h2>
  <ul className="flex flex-col gap-4">
    {[
      'Bezpośredni kontakt ze studentami UEW',
      'Możliwość promocji marki w środowisku akademickim',
      'Wsparcie przy organizacji wydarzeń i inicjatyw studenckich',
      'Doświadczenie organizacyjne Samorządu Studenckiego',
      'Indywidualne podejście do każdej współpracy',
    ].map((item) => (
      <li key={item} className="flex items-start gap-3">
        <span className="shrink-0 mt-1 w-5 h-5 rounded-full bg-primary-light flex items-center justify-center">
          <span className="w-2 h-2 rounded-full bg-primary" />
        </span>
        <span className="text-base text-ssuew-gray-600">{item}</span>
      </li>
    ))}
  </ul>
</div>
```

**Section 3: Cooperation form** — `<section className="py-20 bg-ssuew-gray-100">`
- Container, max-w-2xl form area
- H2: "Formularz współpracy" — `font-display text-display-lg text-ssuew-black mb-8`
- Note paragraph: `<p className="text-[0.85rem] text-ssuew-gray-600 mb-8">Wypełnij formularz, a odezwiemy się do Ciebie w ciągu 2 dni roboczych.</p>`
- Form element: `<form className="flex flex-col gap-6">` — NO action, NO method (Phase 5 wires this)
- 6 form fields:

```tsx
{/* Nazwa firmy */}
<div className="flex flex-col gap-2">
  <label className="text-[0.85rem] font-bold text-ssuew-black">Nazwa firmy *</label>
  <input type="text" placeholder="np. Przykładowa Sp. z o.o." required
    className="rounded-brand-sm border border-ssuew-gray-200 px-4 py-3 text-base text-ssuew-black placeholder:text-ssuew-gray-600 focus:outline-none focus:border-primary transition-colors duration-200" />
</div>

{/* Imię i nazwisko */}
<div className="flex flex-col gap-2">
  <label className="text-[0.85rem] font-bold text-ssuew-black">Imię i nazwisko *</label>
  <input type="text" placeholder="Jan Kowalski" required
    className="rounded-brand-sm border border-ssuew-gray-200 px-4 py-3 text-base text-ssuew-black placeholder:text-ssuew-gray-600 focus:outline-none focus:border-primary transition-colors duration-200" />
</div>

{/* Email */}
<div className="flex flex-col gap-2">
  <label className="text-[0.85rem] font-bold text-ssuew-black">Email *</label>
  <input type="email" placeholder="jan.kowalski@firma.pl" required
    className="rounded-brand-sm border border-ssuew-gray-200 px-4 py-3 text-base text-ssuew-black placeholder:text-ssuew-gray-600 focus:outline-none focus:border-primary transition-colors duration-200" />
</div>

{/* Telefon */}
<div className="flex flex-col gap-2">
  <label className="text-[0.85rem] font-bold text-ssuew-black">Telefon</label>
  <input type="tel" placeholder="+48 000 000 000"
    className="rounded-brand-sm border border-ssuew-gray-200 px-4 py-3 text-base text-ssuew-black placeholder:text-ssuew-gray-600 focus:outline-none focus:border-primary transition-colors duration-200" />
</div>

{/* Rodzaj współpracy */}
<div className="flex flex-col gap-2">
  <label className="text-[0.85rem] font-bold text-ssuew-black">Rodzaj współpracy *</label>
  <select required
    className="rounded-brand-sm border border-ssuew-gray-200 px-4 py-3 text-base text-ssuew-black bg-white focus:outline-none focus:border-primary transition-colors duration-200">
    <option value="">Wybierz rodzaj...</option>
    <option value="sponsoring">Sponsoring</option>
    <option value="wydarzenie">Współorganizacja wydarzeń</option>
    <option value="promocja">Promocja marki</option>
    <option value="inne">Inne</option>
  </select>
</div>

{/* Dodatkowe informacje */}
<div className="flex flex-col gap-2">
  <label className="text-[0.85rem] font-bold text-ssuew-black">Dodatkowe informacje *</label>
  <textarea rows={5} placeholder="Opisz propozycję współpracy..." required
    className="rounded-brand-sm border border-ssuew-gray-200 px-4 py-3 text-base text-ssuew-black placeholder:text-ssuew-gray-600 focus:outline-none focus:border-primary transition-colors duration-200 resize-none" />
</div>
```

Submit button — disabled until Phase 5:
```tsx
{/* TODO: Phase 5 — wire to POST /api/formularz with routing to karol.vogel@ */}
<Button type="submit" disabled aria-disabled="true" className="opacity-60 cursor-not-allowed self-start">
  Wyślij formularz
</Button>
```

**Section 4: Partners** — `<section className="py-20">`
- Container
- H2: "Nasi Partnerzy" — `font-display text-display-lg text-ssuew-black mb-4`
- "Partner Strategiczny" sub-label above BNY:
  ```tsx
  <p className="text-[0.85rem] font-bold text-primary uppercase tracking-widest mb-6">Partner Strategiczny</p>
  <PartnerGrid partners={[partnerStrategiczny]} className="mb-12 max-w-xs" />
  <p className="text-[0.85rem] font-bold text-ssuew-gray-600 uppercase tracking-widest mb-6 mt-8">Partnerzy</p>
  <PartnerGrid partners={partnerzy} />
  ```

Imports:
```tsx
import { MemberCard } from '@/components/cards/MemberCard'
import { PartnerGrid } from '@/components/sections/PartnerGrid'
import { Button } from '@/components/ui/Button'
import { FadeUp } from '@/components/ui/FadeUp'
import { partnerStrategiczny, partnerzy } from '@/data/partnerzy'
```
  </action>
  <verify>
    <automated>grep -c "karol.vogel@samorzad.ue.wroc.pl" "app/(public)/wspolprace/page.tsx" && grep -c "aria-disabled" "app/(public)/wspolprace/page.tsx" && grep -c "PartnerGrid" "app/(public)/wspolprace/page.tsx"</automated>
  </verify>
  <done>
    - app/(public)/wspolprace/page.tsx exists
    - Karol Vogel's email present: karol.vogel@samorzad.ue.wroc.pl
    - Form has aria-disabled="true" on submit button with TODO comment for Phase 5
    - PartnerGrid used twice (strategic + regular)
    - All 5 "Dlaczego warto" bullet points present
    - 6 form fields present: Nazwa firmy, Imię i nazwisko, Email, Telefon, Rodzaj współpracy, Dodatkowe informacje
    - No 'use client' directive
    - grep -c "TODO: Phase 5" "app/(public)/wspolprace/page.tsx" returns 1
  </done>
</task>

</tasks>

<verification>
- `grep -c "karol.vogel@samorzad.ue.wroc.pl" app/\(public\)/wspolprace/page.tsx` → 1
- `grep -c "aria-disabled" app/\(public\)/wspolprace/page.tsx` → 1
- `grep -c "PartnerGrid" app/\(public\)/wspolprace/page.tsx` → 3+ (import + 2 usages)
- `grep -c "511 599 376" app/\(public\)/wspolprace/page.tsx` → 1 (phone in description)
- `grep -rn "use client" app/\(public\)/wspolprace/page.tsx` → 0
- `npx tsc --noEmit` → 0 new errors
</verification>

<success_criteria>
- /wspolprace: Karol Vogel MemberCard with email + phone
- 5-item "Dlaczego warto" bullet list
- UI-only cooperation form with 6 fields, submit disabled (aria-disabled) + TODO Phase 5 comment
- Partner section: BNY strategic + 11 regular partners via PartnerGrid
</success_criteria>

<output>
After completion, create `.planning/phases/04-samorz-d-projekty-partnerzy/04-06-SUMMARY.md`
</output>
