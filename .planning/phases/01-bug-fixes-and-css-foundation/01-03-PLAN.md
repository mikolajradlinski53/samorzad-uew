---
phase: 01-bug-fixes-and-css-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - style.css
  - index.html
  - aktualnosci.html
  - wydarzenia.html
  - zarzad.html
  - projekty.html
  - strefa-studenta.html
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "No style= attribute exists in any HTML file except the Netlify honeypot <p style='display:none'> and the formSuccess/dismiss temp styles (which are tagged FOUND-01 for future extraction)"
    - "No <style> block exists in any HTML <head> (the hero block from index.html is in style.css)"
    - "The hero section renders visually identically before and after the extraction"
  artifacts:
    - path: "style.css"
      provides: "Hero styles (.hero, .hero-bg-circle, .hero-content, .hero-label, etc.), .logo-icon, .footer-logo, .contact-email-link, .social-links--contact, .footer-bottom--subpage, .footer-back-link, .card--centered, .card-text--spaced, .card-footer--center, .campus-map-placeholder"
      contains: ".hero {"
    - path: "index.html"
      provides: "No <style> block in head; logo img has no style= attr; contact email link has no style= attr; social-links div has no style= attr; footer logo img has no style= attr"
      contains: "class=\"logo-icon\""
  key_links:
    - from: "index.html hero section"
      to: "style.css .hero rule"
      via: "class='hero' on <section> element — already present, style block removal makes CSS rule canonical"
      pattern: "\\.hero \\{"
    - from: "logo img tags (all 6 files)"
      to: "style.css .logo-icon rule"
      via: "class='logo-icon' already on every img — inline style removed, class rule provides the same styles"
      pattern: "\\.logo-icon \\{"
---

<objective>
Extract all inline styles from HTML files into named CSS classes in style.css, eliminating every style= attribute and the hero <style> block from index.html head.

Purpose: Inline styles have higher CSS specificity than class rules, which means any redesign changes in style.css are silently overridden. Extracting them now creates a clean, predictable CSS surface for Phase 2 redesign work.
Output: style.css gains all extracted rules; all 6 HTML files lose their style= attributes (except the required Netlify honeypot); index.html loses its <style> block from <head>.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/01-bug-fixes-and-css-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Complete inventory of inline styles to extract (from grep audit) -->

index.html inline styles:
1. Line 43 — logo img: `style="width:40px;height:40px;object-fit:contain;background:transparent;border-radius:0;display:block;"`
   → class `.logo-icon` already present on the img tag. Add rule to style.css.
2. Line 377 — email link: `style="color:var(--primary)"`
   → add class `contact-email-link` to the `<a>` and create rule in style.css.
3. Line 387 — social-links div: `style="margin-top:1.5rem"`
   → add modifier class `social-links--contact` to the div and create rule in style.css.
4. Line 403 — footer logo img: `style="width:40px;height:40px;object-fit:contain;background:transparent;border-radius:0;display:block;"`
   → class `.logo-icon` already present. Same rule as #1 covers this.
5. Lines 12–35 — hero `<style>` block in `<head>`: 24 lines of rules for .hero, .hero-bg-circle, .hero-bg-circle:nth-child(), @keyframes float, .hero-content, .hero-label, .hero-title, .hero-title .accent, #typewriter, .hero-subtitle, .hero-actions, .hero-scroll, .hero-scroll .arrow, @keyframes bounce.
   → Move ALL of these rules verbatim into style.css (append to end of file in a clearly marked section).

aktualnosci.html inline styles:
6. Line 89 — footer-bottom div: `style="border-top:none;padding-top:0"`
   → add class `footer-bottom--subpage` to the div and create rule in style.css.
7. Line 91 — back-link button: `style="color:#fff;border-color:rgba(255,255,255,0.3)"`
   → add class `footer-back-link` to the `<a>` and create rule in style.css.
   (Same pattern repeats in projekty.html line 59/61, zarzad.html line 72/74, wydarzenia.html line 89/91, strefa-studenta.html line 126/128)

projekty.html inline styles:
8. Lines 59, 61 — same as aktualnosci.html #6 and #7 above.

zarzad.html inline styles:
9. Line 53 — section-header div: `style="margin-top:4rem"`
   → add class `section-header--spaced` to the div.
10. Line 60 — section div: `style="padding-top:3rem"` on a `.section` element
    → add class `section--top-padded` to the div.
11. Line 61 — card div: `style="max-width:700px;margin:0 auto;text-align:center;padding:3rem"`
    → add class `card--centered` to the div.
12. Line 63 — card-text: `style="margin-top:1rem"`
    → add class `card-text--spaced` to the `<p>`.
13. Line 64 — card-footer: `style="text-align:center"` and button: `style="display:inline-flex"`
    → add class `card-footer--center` to card-footer div; inline-flex on button → add class `btn--inline-flex`.
14. Lines 72, 74 — same as aktualnosci.html #6 and #7.

strefa-studenta.html inline styles:
15. Line 77 — card div: `style="max-width:700px;margin:0 auto;text-align:center;padding:3rem"` — same as zarzad.html #11 → use `card--centered`.
16. Line 79 — card-footer: `style="text-align:center;margin-top:2rem"` → add class `card-footer--center` (already defined in #13), also add `card-footer--spaced` for margin-top.
17. Line 103 — card div: `style="padding:3rem;text-align:center"` → add class `card--padded-center`.
18. Line 104 — inner div: `style="background:var(--bg-alt);border-radius:var(--radius);padding:4rem;color:var(--text-muted);font-size:1.1rem"` → add class `campus-map-placeholder`.
19. Line 105 — small element: `style="font-size:0.85rem"` → add class `text-sm`.
20. Lines 126, 128 — same as aktualnosci.html #6 and #7.

<!-- EXCEPTION: Do NOT touch these inline styles -->
- `<p style="display:none"><input name="bot-field"></p>` in index.html — Netlify honeypot, MUST stay inline
- Any `style=` attributes added by Plan 01 on #formSuccess and dismiss button (tagged FOUND-01 for later extraction)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all extracted CSS rules to style.css</name>
  <files>style.css</files>

  <read_first>style.css (read the entire file to understand existing rules, find where to append, and avoid duplicate class names)</read_first>

  <action>
Append a clearly delimited section to the end of style.css containing ALL the new classes needed. Use this exact block (copy verbatim, adjust property values only if a rule name already exists in the file with different properties):

```css
/* =============================================
   PHASE 1 EXTRACTED INLINE STYLES (FOUND-01)
   Moved from HTML style= attributes and index.html <style> block
   ============================================= */

/* Logo icon — used in navbar and footer on all pages */
.logo-icon {
  width: 40px;
  height: 40px;
  object-fit: contain;
  background: transparent;
  border-radius: 0;
  display: block;
}

/* Contact section email link */
.contact-email-link {
  color: var(--primary);
}

/* Social links in contact section (extra top margin) */
.social-links--contact {
  margin-top: 1.5rem;
}

/* Subpage footer bottom strip — no border, no top padding */
.footer-bottom--subpage {
  border-top: none;
  padding-top: 0;
}

/* Subpage footer back-to-home link */
.footer-back-link {
  color: #fff;
  border-color: rgba(255, 255, 255, 0.3);
}

/* Centered card with max-width (zarząd info card, strefa-studenta rzecznik card) */
.card--centered {
  max-width: 700px;
  margin: 0 auto;
  text-align: center;
  padding: 3rem;
}

/* Padded centered card (strefa-studenta section) */
.card--padded-center {
  padding: 3rem;
  text-align: center;
}

/* Card text with top spacing */
.card-text--spaced {
  margin-top: 1rem;
}

/* Centered card footer */
.card-footer--center {
  text-align: center;
}

/* Card footer with top margin */
.card-footer--spaced {
  margin-top: 2rem;
}

/* Inline-flex button override (for buttons inside centered card footers) */
.btn--inline-flex {
  display: inline-flex;
}

/* Section header with extra top margin (zarząd rada section) */
.section-header--spaced {
  margin-top: 4rem;
}

/* Section with top-only padding (zarząd info section) */
.section--top-padded {
  padding-top: 3rem;
}

/* Campus map placeholder block */
.campus-map-placeholder {
  background: var(--bg-alt);
  border-radius: var(--radius);
  padding: 4rem;
  color: var(--text-muted);
  font-size: 1.1rem;
}

/* Small utility text */
.text-sm {
  font-size: 0.85rem;
}

/* =============================================
   HERO SECTION (moved from index.html <style> block)
   ============================================= */

.hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  padding-top: 80px;
  background: linear-gradient(145deg, #EAF5FF 0%, #F7FBFF 50%, #fff 100%);
  position: relative;
  overflow: hidden;
}

.hero-bg-circle {
  position: absolute;
  border-radius: 50%;
  opacity: 0.18;
  animation: float 6s ease-in-out infinite;
}

.hero-bg-circle:nth-child(1) {
  width: 420px;
  height: 420px;
  background: var(--primary);
  top: -80px;
  right: -100px;
  animation-delay: 0s;
}

.hero-bg-circle:nth-child(2) {
  width: 260px;
  height: 260px;
  background: var(--primary-dark);
  bottom: 80px;
  right: 200px;
  animation-delay: 2s;
}

.hero-bg-circle:nth-child(3) {
  width: 160px;
  height: 160px;
  background: var(--primary);
  top: 200px;
  left: -40px;
  animation-delay: 4s;
}

@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-20px); }
}

.hero-content {
  position: relative;
  z-index: 1;
  max-width: 680px;
}

.hero-label {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  background: var(--primary-light);
  color: var(--primary);
  font-size: 0.8rem;
  font-weight: 700;
  padding: 0.4rem 1.1rem;
  border-radius: 100px;
  margin-bottom: 1.5rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.hero-title {
  margin-bottom: 1.5rem;
  color: var(--text);
}

.hero-title .accent {
  color: var(--primary);
  display: inline-block;
}

#typewriter {
  transition: opacity 0.3s ease;
  display: inline-block;
}

.hero-subtitle {
  font-size: 1.1rem;
  max-width: 540px;
  margin-bottom: 2.5rem;
  line-height: 1.7;
}

.hero-actions {
  display: flex;
  gap: 1rem;
  flex-wrap: wrap;
}

.hero-scroll {
  position: absolute;
  bottom: 2rem;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.4rem;
  color: var(--text-muted);
  font-size: 0.75rem;
  animation: bounce 2s infinite;
}

.hero-scroll .arrow {
  font-size: 1.2rem;
}

@keyframes bounce {
  0%, 100% { transform: translateX(-50%) translateY(0); }
  50% { transform: translateX(-50%) translateY(6px); }
}
```

Before writing, search style.css for any existing `.logo-icon` rule. If one already exists with different properties, merge rather than duplicate — the extracted version wins (it is the authoritative declaration).
  </action>

  <acceptance_criteria>
- `grep -n '\.logo-icon {' style.css` returns at least 1 result
- `grep -n '\.hero {' style.css` returns at least 1 result
- `grep -n '@keyframes float' style.css` returns at least 1 result
- `grep -n '@keyframes bounce' style.css` returns at least 1 result
- `grep -n '\.footer-bottom--subpage' style.css` returns at least 1 result
- `grep -n '\.campus-map-placeholder' style.css` returns at least 1 result
- `grep -n '\.card--centered' style.css` returns at least 1 result
  </acceptance_criteria>

  <verify>
    <automated>grep -c "\.logo-icon\|\.hero {\|@keyframes float\|\.footer-bottom--subpage\|\.card--centered" style.css</automated>
  </verify>
  <done>style.css contains all extracted classes and the full hero section rules. No rule is duplicated.</done>
</task>

<task type="auto">
  <name>Task 2: Remove all inline styles from all 6 HTML files</name>
  <files>index.html, aktualnosci.html, wydarzenia.html, zarzad.html, projekty.html, strefa-studenta.html</files>

  <read_first>
  - index.html (full file — to find hero style block in head, logo img, email link, social-links div, footer logo)
  - aktualnosci.html (lines 85–97 — footer section with inline styles)
  - zarzad.html (lines 50–75 — section-header, section, card, footer)
  - strefa-studenta.html (lines 74–130 — cards and footer)
  - eventos.html and projekty.html (lines 55–65 — footer section)
  </read_first>

  <action>
Apply the following changes to each HTML file. In every case: remove the `style="..."` attribute and add (or replace with) the CSS class listed. Do NOT alter any other attributes, content, or whitespace beyond what is specified.

**index.html:**

1. Remove the entire `<style> ... </style>` block from `<head>` (lines 12–35). The hero rules are now in style.css. The document `<head>` should jump directly from `<link rel="stylesheet" href="style.css">` to `</head>`.

2. Line 43 — navbar logo img: remove `style="width:40px;height:40px;object-fit:contain;background:transparent;border-radius:0;display:block;"`. The class `logo-icon` is already present and now provides these styles via style.css.

3. Line 377 — email link `<a href="mailto:...">`: remove `style="color:var(--primary)"` and add class `contact-email-link`. Result: `<a href="mailto:kontakt@samorzad.ue.wroc.pl" class="contact-email-link">`.

4. Line 387 — social-links div: remove `style="margin-top:1.5rem"` and add class `social-links--contact`. Result: `<div class="social-links social-links--contact">`.

5. Line 403 — footer logo img: remove `style="width:40px;height:40px;object-fit:contain;background:transparent;border-radius:0;display:block;"`. Class `logo-icon` is already present.

**aktualnosci.html:**

6. Logo img (line 12): remove same `style=` attribute. `.logo-icon` already present.
7. Footer-bottom div (line 89): remove `style="border-top:none;padding-top:0"` and add class `footer-bottom--subpage`. Result: `<div class="footer-bottom footer-bottom--subpage">`.
8. Back-link `<a>` (line 91): remove `style="color:#fff;border-color:rgba(255,255,255,0.3)"` and add class `footer-back-link`. Result: `<a href="index.html" class="btn btn-outline btn-sm footer-back-link">`.

**wydarzenia.html:**
9. Logo img (line 12): remove `style=`.
10. Footer-bottom (line 89): same as aktualnosci step 7.
11. Back-link (line 91): same as aktualnosci step 8.

**projekty.html:**
12. Logo img (line 12): remove `style=`.
13. Footer-bottom (line 59): same pattern.
14. Back-link (line 61): same pattern.

**zarzad.html:**
15. Logo img (line 12): remove `style=`.
16. Section-header div (line 53): remove `style="margin-top:4rem"` and add class `section-header--spaced`. Result: `<div class="section-header fade-up section-header--spaced">`.
17. Section div (line 60): remove `style="padding-top:3rem"` and add class `section--top-padded`. Result: `<div class="section section--top-padded">`.
18. Card div (line 61): remove `style="max-width:700px;margin:0 auto;text-align:center;padding:3rem"` and add class `card--centered`. Result: `<div class="card fade-up card--centered">`.
19. Card-text p (line 63): remove `style="margin-top:1rem"` and add class `card-text--spaced`. Result: `<p class="card-text card-text--spaced">`.
20. Card-footer div (line 64): remove `style="text-align:center"` and add class `card-footer--center`. Result: `<div class="card-footer card-footer--center">`.
21. Button in card-footer (line 64): remove `style="display:inline-flex"` and add class `btn--inline-flex`.
22. Footer-bottom (line 72): same as aktualnosci step 7.
23. Back-link (line 74): same as aktualnosci step 8.

**strefa-studenta.html:**
24. Logo img (line 12): remove `style=`.
25. Card div (line 77): remove `style="max-width:700px;margin:0 auto;text-align:center;padding:3rem"` and add class `card--centered`.
26. Card-footer div (line 79): remove `style="text-align:center;margin-top:2rem"` and add classes `card-footer--center card-footer--spaced`. Result: `<div class="card-footer card-footer--center card-footer--spaced">`.
27. Card div (line 103): remove `style="padding:3rem;text-align:center"` and add class `card--padded-center`.
28. Inner div (line 104): remove `style="background:var(--bg-alt);border-radius:var(--radius);padding:4rem;color:var(--text-muted);font-size:1.1rem"` and add class `campus-map-placeholder`.
29. Small element (line 105): remove `style="font-size:0.85rem"` and add class `text-sm`. Result: `<small class="text-sm">`.
30. Footer-bottom (line 126): same as aktualnosci step 7.
31. Back-link (line 128): same as aktualnosci step 8.

**EXCEPTION — do NOT remove these `style=` attributes:**
- `<p style="display:none"><input name="bot-field"></p>` — Netlify honeypot (must stay)
- Any `style="display:none"` or `style="display:block"` on `#formSuccess` set by JavaScript at runtime — these are controlled by JS, not authored inline styles
- The `style=` attributes on `#formSuccess` div and `#formSuccessDismiss` button added in Plan 01 (tagged with `<!-- FOUND-01: extract -->` comment) — leave those for Phase 2
  </action>

  <acceptance_criteria>
- `grep -rn 'style="' index.html` — must return ONLY the honeypot `<p style="display:none">` and any Plan-01-added formSuccess styles (tagged FOUND-01). Zero other results.
- `grep -c 'style="' aktualnosci.html` returns 0
- `grep -c 'style="' wydarzenia.html` returns 0
- `grep -c 'style="' zarzad.html` returns 0
- `grep -c 'style="' projekty.html` returns 0
- `grep -c 'style="' strefa-studenta.html` returns 0
- `grep -n '<style>' index.html` returns 0 results (no inline style block in head)
- `grep -n 'class="logo-icon"' index.html` returns 2 results (navbar + footer — both still have the class attribute without the style attribute)
- `grep -n 'footer-bottom--subpage' aktualnosci.html` returns 1 result
- `grep -n 'card--centered' zarzad.html` returns 1 result
  </acceptance_criteria>

  <verify>
    <automated>grep -rc 'style="' aktualnosci.html wydarzenia.html zarzad.html projekty.html strefa-studenta.html</automated>
  </verify>
  <done>All 5 subpages have zero style= attributes. index.html has zero style= attributes outside the honeypot exception and Plan-01 tagged items. index.html head has no &lt;style&gt; block. Hero section renders correctly via style.css rules.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rn '<style>' *.html` — must return 0 results across all HTML files
2. `grep -rn 'style="' aktualnosci.html zarzad.html eventos.html projekty.html strefa-studenta.html` — must return 0 results
3. `grep -n '.hero {' style.css` — must return 1 result
4. Open index.html in browser — hero section must render with gradient background, floating circles, and scroll indicator (same as before)
</verification>

<success_criteria>
- Zero `<style>` blocks in any HTML `<head>`
- Zero `style=` attributes in the 5 subpage files
- Only permitted `style=` exceptions remain in index.html (honeypot + Plan-01 tagged items)
- style.css contains all hero rules and all extracted utility classes
- Visual appearance of the hero section is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-bug-fixes-and-css-foundation/01-03-SUMMARY.md`
</output>
